import 'package:flutter/material.dart';

class PauseAndPlay extends StatelessWidget {
  const PauseAndPlay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.play_circle_fill_sharp,
      size: 60,
    );
  }
}
